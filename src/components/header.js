import { Box, Typography } from "@mui/material";

export const Header = () => {
  return (
    <Box>
      <Box>
        <Typography variant="h3">Andrew's Header</Typography>
      </Box>
    </Box>
  );
};
